#include <iostream>
#include <string>
using namespace std;

int main()
{
	string a;
	cin >> a;

	int result = 0;
	string num;
	bool minus = false;

	for (int i = 0; i <= a.size(); i++) {
		if (a[i] == '-' || a[i] == '+' || i == a.size()) {
			if (minus) {
				result -= stoi(num);
				num = "";
			}
			else {
				result += stoi(num);
				num = "";
			}
		}
		else {
			num += a[i];
		}
		if (a[i] == '-') {
			minus = true;
		}
	}
	cout << result;
}