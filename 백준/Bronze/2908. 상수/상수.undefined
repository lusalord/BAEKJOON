#include <iostream>
using namespace std;
int main() 
{
	string n, m;

	cin >> n >> m;

	string a;
	string b;

	for(int i = n.size() - 1; i >= 0; i--) 
	{
		a += n[i];
	}
	for(int i = m.size() - 1; i >= 0; i--) 
	{
		b += m[i];
	}

	if(a > b) 
	{
		cout << a << endl;
	} 
	else 
	{
		cout << b << endl;
	}
}